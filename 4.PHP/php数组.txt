------------------------------------

1--》数组排序相关函数
------------------------------------
sort() 函数用于对数组单元从低到高进行排序。

rsort() 函数用于对数组单元从高到低进行排序。

asort() 函数用于对数组单元从低到高进行排序并保持索引关系。

arsort() 函数用于对数组单元从高到低进行排序并保持索引关系。

ksort() 函数用于对数组单元按照键名从低到高进行排序。

krsort() 函数用于对数组单元按照键名从高到低进行排序。
------------------------------------

2--》以字符串作为键名的数组，称为（关联数组）以数字作为键名的数组，称为（数字索引数组）
------------------------------------
------------------------------------

3--》获取已经上传文件的大小，使用（$_FILES["userfile"]["size"] ）数组
------------------------------------
------------------------------------

4--》使用（list() ）和each()函数遍历一个数组
------------------------------------
------------------------------------

5--》删除数组中的一个元素，使用（unset() ）例如
------------------------------------
//销毁单个数组元素
unset ($bar['quux']);
------------------------------------

6--》下面哪个选项没有将 john 添加到users 数组中？ (b,d)
------------------------------------
a) $users[] = ‘john’;
(b) array_add($users,’john’);
(c) array_push($users,‘john’);
(d) $users ||= ‘john’;
------------------------------------

7--》$arr = array('james', 'tom', 'symfony'); 请打印出第一个元素的值
------------------------------------
print_r($arr[0]);
reset($arr);
print_r(current($arr));
print_r(array_shift($arr));

------------------------------------

8--》输出结果为 6634
------------------------------------
<?php

$ar=array('0'=>9,'ok'=>2,3,4); 
//这个数组 应该为array('0'=>9,'ok'=>2,'1'=>3,'2'=>4);  $key为0的时候是可以等于Ok字符串的
foreach($ar as $key=>$val){
print_r($key);
print_r('-');
  print_r($key=='ok'?6:$val);
}


























